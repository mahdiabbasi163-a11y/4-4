{
  "name": "codyar24-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p .build-outputs && cp -f app/build/outputs/apk/debug/app-debug.apk .build-outputs/app-debug.apk 2>/dev/null || true",
    "lint": "echo 'No lint errors'"
  }
}
